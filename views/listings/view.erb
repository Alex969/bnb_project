<!DOCTYPE html>
  <html>
    <head>
    <link rel="stylesheet" type="text/css" href="/style.css">
    </head>
    <body>

    <div class="banner">
      <div class="banner_left">
        <a href='/register'>Register</a> | 
        <a href='/login'>Login</a>
      </div>
      <div class="banner_right">  
        <a href='/'>View all Listings</a> | 
        <a href='/requests'>View all requests</a>
      </div>
    </div>

    <h1>
      <%= @current_listing.title %>
    </h1>

    <h2>
      £<%= @current_listing.price_per_night %> per night
    </h2>
    <br><br>
    <p>
      <%= @current_listing.description %>
    </p>
    <h2 style="color:red;">
    <%= flash[:notice] %>
    </h2>
    <ul>
        <%@available_dates.each do |booking|%>
          <div class="column">
            <%=booking.date%>
          </div>
          <div class="column">
            <form action="/view" style="text-align:left;" method="post">
              <input type='hidden' name='listing_id' value='<%=@current_listing.id%>'/>
              <input type='hidden' name='booking_id' value='<%=booking.id%>'/>
         
              <input type="submit" value="Request Booking"/>
            </form>
          </div>
        <%end%>
    </ul>
    

    </body>
  </html>
  